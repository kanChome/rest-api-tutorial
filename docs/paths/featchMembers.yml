get:
  tags:
    - member
  operationId: featchMembers
  description: 会員を全件取得します
  responses:
    '200':
      description: 成功時
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../index.yml#/components/schemas/member"